[build]
  functions = "functions"
  command = "echo 'this is only here so that netlify deploys functions within folders'"
[[redirects]]
  from = "/"
  query = {query = ":query"}
  to = "/.netlify/functions/goto?query=:query"
  status = 200